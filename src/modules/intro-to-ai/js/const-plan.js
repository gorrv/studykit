    /* ================= W3 · planning ================= */
    const PLAN = [
      { q: 'Under the <strong>closed-world assumption</strong>, what happens to a relation you did not list in the initial state?', a: 'It is assumed to be false',
        c: ['It is assumed to be false', 'It is assumed to be true', 'It is unknown and the planner branches on it', 'It causes a syntax error'],
        e: 'This keeps the initial state a short list of positives. The price is that the model must be <strong>complete</strong> — a forgotten <code>connected</code> fact silently becomes a road that does not exist.' },
      { q: 'A PDDL <strong>domain</strong> file contains…', a: 'The predicates and actions — the general description of the world',
        c: ['The predicates and actions — the general description of the world', 'The objects, initial state and goal', 'The plan produced by the planner', 'The numeric fluent values'],
        e: 'One domain, many problem files. Test: <em>would it still be true for a different puzzle in the same world?</em> If yes it belongs in the domain.' },
      { q: 'Which version of PDDL adds <strong>numbers and time</strong>?', a: 'PDDL 2.1', c: ['PDDL 2.1', 'PDDL', 'PDDL 3', 'PDDL+'],
        e: 'PDDL propositional · <strong>PDDL 2.1 numbers + time</strong> · PDDL 3 preferences · PDDL+ continuous change, processes, events.' },
      { q: 'How is a delete effect written in PDDL?', a: '(not (fact)) inside the :effect',
        c: ['(not (fact)) inside the :effect', 'In a separate :delete section', 'With a minus sign before the fact', 'Delete effects are inferred automatically'],
        e: 'There is no separate delete list in PDDL — the effect is one <code>(and …)</code> containing both added facts and <code>(not …)</code> wrappers.' },
      { q: 'Which condition type must hold for the <em>entire duration</em> of a durative action?', a: 'over all',
        c: ['over all', 'at start', 'at end', ':duration'],
        e: 'Three condition types — <code>at start</code>, <code>over all</code>, <code>at end</code> — but only <strong>two</strong> effect types: <code>at start</code> and <code>at end</code>.' },
      { q: 'In the lift example, why is <code>(not (lift-at ?lift ?f1))</code> applied <strong>at start</strong>?', a: 'The lift leaves f1 immediately and is nowhere until the end',
        c: ['The lift leaves f1 immediately and is nowhere until the end', 'Because deletes always happen at the start in PDDL', 'To make the action reversible', 'So the action can be interrupted'],
        e: 'That gap between the start-delete and the end-add is what allows <strong>other actions to run concurrently</strong> — the source of real parallelism in temporal plans.' },
      { q: 'What does forward-chain planning compute for each applicable action?', a: 'S′ = S − {delete effects} + {add effects}',
        c: ['S′ = S − {delete effects} + {add effects}', 'S′ = S + {delete effects} − {add effects}', 'S′ = S ∪ preconditions', 'S′ = the goal minus S'],
        e: 'Delete first, then add — the order matters when an action both deletes and adds the same fact. A goal state is any state <em>containing</em> the goal conditions.' },
      { q: 'What relaxation does the <strong>RPG</strong> heuristic use?', a: 'It ignores the delete effects of each action',
        c: ['It ignores the delete effects of each action', 'It ignores the preconditions of each action', 'It assumes all actions take unit time', 'It ignores half the goals'],
        e: 'With no deletes, facts can only ever become true — nothing can be undone, so no backtracking is needed and the relaxed problem is polynomial. Every real plan is still a relaxed plan, so its length is a lower bound.' },
      { q: 'In the RPG, what is <code>f(n+1)</code>?', a: 'f(n) plus all the add effects of the actions in a(n+1)',
        c: ['f(n) plus all the add effects of the actions in a(n+1)', 'The add effects of a(n+1) only', 'f(n) minus the delete effects of a(n+1)', 'The preconditions of the actions in a(n+1)'],
        e: 'Fact layers <strong>only grow</strong>. That guarantees termination, and if the fixpoint still lacks a goal then <strong>no real plan exists</strong> — a free dead-end detector.' },
      { q: 'In the RPG heuristic, <code>h(s) := Σ |O<sub>i</sub>|</code> counts…', a: 'The number of actions in the relaxed plan',
        c: ['The number of actions in the relaxed plan', 'The number of fact layers', 'The number of action layers', 'The number of goals'],
        e: 'In the demo m = 2 layers but <strong>h = 3</strong>, because O₀ holds two actions. Summing |O<sub>i</sub>| estimates the <em>sequential</em> plan length. Confusing h with the layer count is the standard mistake.' },
      { q: 'Which planner is the RPG heuristic most associated with?', a: 'FF', c: ['FF', 'Fast Downward', 'POPF', 'Gamer'],
        e: 'Hoffmann &amp; Nebel 2001, "The FF Planning System: Fast Plan Generation through Heuristic Search", <em>JAIR</em> vol. 14.' },
      { q: 'How often does a planner build the RPG?', a: 'Once for every node added to the open list',
        c: ['Once for every node added to the open list', 'Once, at the start', 'Once per action layer', 'Only when the search gets stuck'],
        e: 'The heuristic is an <strong>entire sub-problem solved from scratch per node</strong> — far more expensive than Week 1’s straight-line distance, and worth it because the reduction in nodes expanded is larger still.' }
    ];
    PLAN.forEach(d => QZ_GEN.push({ topic: 'planning', make: () => ({
      topic: 'planning · concepts', kind: 'choice', prompt: d.q, choices: d.c.slice(), answer: d.a,
      check: textCheck(d.a), explain: d.e }) }));
